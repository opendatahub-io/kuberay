name: E2E Tests

on:
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - dev
      - 'release-*'

concurrency:
  group: ${{ github.head_ref }}-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  odh-kuberay-client-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up gotestfmt
        uses: gotesttools/gotestfmt-action@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup and start KinD cluster
        uses: ./.github/workflows/actions/kind

      - name: Deploy Kuberay operator
        id: deploy
        run: |
          echo "Deploying Kuberay operator"
          cd ray-operator

          IMG=quay.io/opendatahub/kuberay-operator:dev
          make docker-build -e IMG="${IMG}" -e ENGINE=docker
          kind load docker-image ${IMG}

          make deploy -e IMG="${IMG}"
          kubectl wait --timeout=90s --for=condition=Available=true deployment -n default kuberay-operator

      - name: Install dependencies
        run: |
          cd clients/odh-kuberay-client
          pip install -e .
          pip install pytest

      - name: Run Python client integration tests with pytest
        run: |
          pytest -s clients/odh-kuberay-client/python_client_test/

      - name: Print KubeRay operator logs
        run: |
          echo "Printing KubeRay operator logs"
          kubectl logs -n default --tail -1 -l app.kubernetes.io/name=kuberay-operator app.kubernetes.io/name=kuberay | tee ./kuberay-operator.log

      - name: Upload logs
        uses: actions/upload-artifact@v4
        with:
          name: logs
          retention-days: 10
          path: |
            **/*.log
